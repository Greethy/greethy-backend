spring:
  application:
    name: user-services
  data:
    mongodb:
      database: GreethyDB
      host: localhost
      port: 27017
  config:
    import: optional:configserver:http://localhost:8888
springdoc:
  swagger-ui:
    use-root-path: true
  api-docs:
    version: openapi_3_1
  show-actuator: true

