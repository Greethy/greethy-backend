spring:
  application:
    name: user-services
  data:
    mongodb:
      database: GreethyDB
      host: localhost
      port: 27017
  config:
    import: optional:configserver:http://localhost:8888
