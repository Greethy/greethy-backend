server:
  port: 8222
eureka:
  server:
    host: localhost:8761
  client:
    service-url:
      defaultZone: http://${eureka.server.host}/eureka
    register-with-eureka: true
    fetch-registry: false
management:
  endpoints:
    web:
      exposure:
        include: gateway