server:
  port: 8888
spring:
  application:
    name: config-server
  profiles:
    active: native, vault
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config-repo
        vault:
          host: 127.0.0.1
          port: 8200
          authentication: token
          token: hvs.qwl5tMoLElmgMVZLGyKvK14k
  main:
    allow-bean-definition-overriding: true
management:
  health:
    livenessstate.enabled: true
    readinessstate.enabled: true
  endpoint:
    health:
      probes:
        enabled: true